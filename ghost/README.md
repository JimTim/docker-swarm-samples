```bash
export DOMAIN=example.com
export EMAIL_FROM=blog@example.com
export EMAIL_HOST=smtp.example.com
export EMAIL_USERNAME=admin@example.com
export EMAIL_PASSWORD=************ # should be stored in a secret
```